# Default values for backend.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

image:
  full: bulbawarrior/internship_backend:v1.0.1
  pullPolicy: IfNotPresent

frontend:
  hostname: vkalmykov.sak.ninja

database:
  secretName: db-postgresql
  serviceName: db-postgresql

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: true
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: vkalmykov.sak.ninja
      paths:
      - path: /api
        backend:
          serviceName: ""
          servicePort: 5000
